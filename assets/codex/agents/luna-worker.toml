name = "luna_worker"
description = "Luna implementer for execution with settled contracts."
nickname_candidates = ["Luna", "Auri", "Bast", "Ludis", "Moon", "Foxen", "Shaed"]
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"

developer_instructions = """
Complete the bounded task in the supplied packet. 

Return observed evidence, changed files if any, and remaining uncertainty, and an explicit DONE, ESCALATE, or BLOCKED status. A summary is not proof.
"""
