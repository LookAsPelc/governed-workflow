# Portable governance baseline. Only the agent routing knobs below are
# portable policy; root model, memories, Desktop UX, and request-input
# preferences remain user-owned.

[agents]
enabled = true
max_threads = 4
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "medium"
